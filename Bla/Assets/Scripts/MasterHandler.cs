using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class MasterHandler {
    public static PlayArea playArea = new PlayArea(new Vector2 (0,0), new Vector2(40 * 1.777f,40));
}
