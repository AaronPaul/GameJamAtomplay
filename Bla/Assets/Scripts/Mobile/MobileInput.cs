using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MobileInput : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		/*
        range = background width  

        foreach touch
        {
            if touch in range
                set pos to pos
                calculate angle
                set pressed = true
        }
         
         */
	}
}
